// NOTE: Not sure if we need this in TS

export namespace TwitchIt.API.Core.Enums {
  /**
   * Enum representing the available throttle types.
   */
  export enum ThrottleType {
    MessageTooShort,
    MessageTooLong
  }
}